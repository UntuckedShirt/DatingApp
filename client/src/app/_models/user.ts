// interfaces in ts are different to interfaces in c#. when
// interfaces are used in ts its used to describe a type of 
// something

export interface User{
    username: string;
    token: string;
}